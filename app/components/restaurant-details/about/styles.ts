import { StyleSheet } from 'react-native';
import { FontSizes } from '../../../globals';

const styles = StyleSheet.create({
    textInfoContainer: { marginVertical: 10, padding: 10 },
    restaurantImage: { width: '100%', height: 180 },
    restaurantNameText: { fontWeight: 'bold', fontSize: FontSizes.fontXLarge },
});

export default styles;
